"""Bundled WeightsLab examples.

Ships with the package so the ``weightslab start example`` CLI can resolve and
run them from an installed location. ``examples.utils`` also holds shared
support code (e.g. baseline models) imported by the examples and tests.
"""
